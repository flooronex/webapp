/**
 * UI Components exports
 * Simple, reusable UI primitives built on Radix UI
 */

// Button
export { Button, buttonVariants, IconButton, iconButtonVariants } from "./button";

// Collapsible
export { Collapsible, CollapsibleRoot, CollapsibleTrigger, CollapsibleContent } from "./collapsible";

// Input
export { Input, inputVariants } from "./input";

// Textarea
export { Textarea, textareaVariants } from "./textarea";

// Select
export {
    Select,
    SelectTrigger,
    SelectValue,
    SelectContent,
    SelectItem,
} from "./select";

// Tooltip
export {
    Tooltip,
    TooltipContent,
    TooltipProvider,
    TooltipTrigger,
} from "./tooltip";

// Tabs
export {
    Tabs,
    TabsList,
    TabsTrigger,
    TabsContent,
} from "./Tabs";

// Toggle
export { Toggle } from "./Toggle";
