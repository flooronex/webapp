// Define the structure for Contact FAQ items
export interface ContactFaqItem {
    id: string; // Translation key (q1, q2, etc.)
}

// Define the FAQ data with translation keys
export const contactFaqData: ContactFaqItem[] = [
    { id: "q1" },
    { id: "q2" },
    { id: "q3" },
];
